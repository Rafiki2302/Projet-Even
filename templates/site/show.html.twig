{% extends 'base.html.twig' %}

{% block title %}Site{% endblock %}

{% block body %}

    <div class="str-main-container">
        <h1>Site</h1>

        <table class="table table-striped str-table-show">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ site.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ site.nom }}</td>
                </tr>
            </tbody>
        </table>

        <div class="str-button-link str-container-button">
            <div class="str-a-position">
                <a href="{{ path('site_index') }}"><i class="fa fa-reply"></i> Retour à la liste</a>
            </div>

            <div class="str-a-position">
                <a href="{{ path('site_edit', {'id': site.id}) }}"><i class="fa fa-pencil"></i> Modifier</a>
            </div>

            <div>
                {{ include('site/_delete_form.html.twig') }}
            </div>

        </div>
    </div>
{% endblock %}
