{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}

    <div class="str-main-container">
        <h1>Ville</h1>

        <table class="table table-striped str-table-show">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ ville.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ ville.nom }}</td>
                </tr>
                <tr>
                    <th>CodePostal</th>
                    <td>{{ ville.codePostal }}</td>
                </tr>
            </tbody>
        </table>

        <div class="str-button-link str-container-button">

            <div class="str-a-position">
                <a href="{{ path('ville_index') }}"><i class="fa fa-reply"></i> Retour à la liste</a>
            </div>

            <div class="str-a-position">
                <a href="{{ path('ville_edit', {'id': ville.id}) }}"><i class="fa fa-pencil"></i> Modifier</a>
            </div>

            <div>
                {{ include('ville/_delete_form.html.twig') }}
            </div>

        </div>
    </div>
{% endblock %}
